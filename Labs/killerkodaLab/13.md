# here the kubeapiserver is failing
we confirm it by 
- k get pods or k get nodes
we check the total cpu available.
- nproc or lscpu
1000m is the total cpu we have, 20% of it is 200m
 so
 requests:
  cpu: 200m
 limits:
  cpu: 200m
the pod will restart in 1-2 min,
check the process
- crictl ps -a
as the kubeapi server is running.
you can use the kubectl commands and also the controlplane will be running again.
