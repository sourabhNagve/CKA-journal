k create priorityclass high-priority --value=999999 --global-default=false --preemption-policy=PreemptLowerPriority
k edit deployments.apps -n priority acme-log-forwarde (just add the priorityClass: high-priority)